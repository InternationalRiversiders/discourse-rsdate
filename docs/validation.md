> 本文保留私有演练的历史记录；本插件现已正式上线，旧入口已切换。当前状态见 [最终复核](final-review.md)。

# 私有测试验证（2026-09-21）

此次仅部署到隔离私有测试容器，生产 web_a / web_b 未安装本插件，旧 RSDate 仍运行。

- 真实快照：408 份资料，6 模块 / 37 题 / 185 选项，2,068 份模块提交 / 12,871 个答案，19 次发布，326 条历史结果，其中 144 条为当前结果。
- 408 个引用账号全部能映射；只读核对了生产论坛对应组及成员授权，未修改生产论坛。
- 导入先执行回滚演练，再导入空目标；资料、日期、问卷、答案、向量、有效性、匹配说明及历史共 79,565 项检查通过。真实目标通知和运行任务均为 0。
- 直接执行旧版 TypeScript 匹配代码作为对照：221 人有效池、1,165 条候选边，逐边分数和模块分量一致；最终 12 对及总分 390 完全一致，197 人未匹配。比较过程只读取导出文件及已导入向量，无外部 API 调用。
- 业务回归：12 个测试、106 项断言，覆盖主动报名、字段验证、问卷更新失效、指定配对规则、幂等、延期定时发布、撤回与历史、权限、只读、账号数据清理和模块档案删除。
- Blossom 验证：2 个测试、1,202 项断言；随机图与穷举、旧实现对照，以及奇数环和最大人数优先案例通过。
- 浏览器：真实登录和 API，无接口模拟；资料编辑、问卷保存、后退/前进、报名/暂停、问卷管理增改删、预览、整池发布、指定配对、撤回、通知、本人的数据导出及权限检查通过。
- 真实快照页面：管理员只读；修改接口返回 422，无操作表单；参与者搜索及分页通过。
- 样式：深浅色切换、320 / 390 / 768 / 1440px；最后对主页、资料、问卷、结果、管理员表单共 20 种布局复核，无横向溢出，浏览器 JavaScript 错误为 0。
- 插件目录凭据格式扫描未发现密钥；真实导出、账号凭据、运行日志及截图均保存在仓库外受限目录。

私人测试入口沿用原论坛：13001 为可操作的虚构账号演示，13000 为管理员真实数据只读核对。后台演示任务严格限制为隔离演示数据库，定时自动发布关闭、向量密钥为空。长驻演示 runner 禁用每轮查询缓存，避免错过新提交的任务。

正式切换前仍需：建立插件远程仓库、固定构建版本、冻结旧端写入、导出最终数据并重新核对、备份生产论坛、导入空的生产插件表、安排旧域名重定向和唯一的定时发布入口。私有演示不替代这些上线步骤。

## 正式发布准备

发布前新增旧链接映射与定时接管起点保护；回归扩展为 14 个测试、120 项断言并全部通过。500 人完全图（124,750 条候选边）求解得到 250 对，约 1.1 秒。旧版最后一次定时发布为 2026-07-12；正式接管从 2026-09-27 20:00 Asia/Shanghai 开始，不补发停滞周期。
